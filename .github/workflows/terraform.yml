name: 'Terraform CI/CD (Sequential)'

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read
  id-token: write

env:
  WIF_PROVIDER: 'projects/1040065388093/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
  SERVICE_ACCOUNT: 'terraform-sa@prj-sh-cicd-499518.iam.gserviceaccount.com'

jobs:
  # 1. BOOTSTRAP
  bootstrap:
    name: 'TF: bootstrap'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bootstrap
    
    steps:
    - uses: actions/checkout@v4
    - uses: google-github-actions/auth@v3
      with:
        workload_identity_provider: ${{ env.WIF_PROVIDER }}
        service_account: ${{ env.SERVICE_ACCOUNT }}
    - uses: hashicorp/setup-terraform@v4
    - run: terraform init
    - run: terraform fmt -check
    - if: github.event_name == 'pull_request'
      run: terraform plan -no-color
    - if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false

  # 2. ORG (Waits for bootstrap)
  org:
    name: 'TF: org'
    runs-on: ubuntu-latest
    needs: bootstrap
    defaults:
      run:
        working-directory: org
        
    steps:
    - uses: actions/checkout@v4
    - uses: google-github-actions/auth@v3
      with:
        workload_identity_provider: ${{ env.WIF_PROVIDER }}
        service_account: ${{ env.SERVICE_ACCOUNT }}
    - uses: hashicorp/setup-terraform@v4
    - run: terraform init
    - run: terraform fmt -check
    - if: github.event_name == 'pull_request'
      run: terraform plan -no-color
    - if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false

  # 3. IAM (Waits for org)
  iam:
    name: 'TF: iam'
    runs-on: ubuntu-latest
    needs: org
    defaults:
      run:
        working-directory: iam
        
    steps:
    - uses: actions/checkout@v4
    - uses: google-github-actions/auth@v3
      with:
        workload_identity_provider: ${{ env.WIF_PROVIDER }}
        service_account: ${{ env.SERVICE_ACCOUNT }}
    - uses: hashicorp/setup-terraform@v4
    - run: terraform init
    - run: terraform fmt -check
    - if: github.event_name == 'pull_request'
      run: terraform plan -no-color
    - if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false

  # 4. NETWORKING (Waits for iam)
  networking:
    name: 'TF: networking'
    runs-on: ubuntu-latest
    needs: iam
    defaults:
      run:
        working-directory: networking
        
    steps:
    - uses: actions/checkout@v4
    - uses: google-github-actions/auth@v3
      with:
        workload_identity_provider: ${{ env.WIF_PROVIDER }}
        service_account: ${{ env.SERVICE_ACCOUNT }}
    - uses: hashicorp/setup-terraform@v4
    - run: terraform init
    - run: terraform fmt -check
    - if: github.event_name == 'pull_request'
      run: terraform plan -no-color
    - if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false

  # 5. PROJECTS (Waits for networking)
  projects:
    name: 'TF: projects'
    runs-on: ubuntu-latest
    needs: networking
    defaults:
      run:
        working-directory: projects
        
    steps:
    - uses: actions/checkout@v4
    - uses: google-github-actions/auth@v3
      with:
        workload_identity_provider: ${{ env.WIF_PROVIDER }}
        service_account: ${{ env.SERVICE_ACCOUNT }}
    - uses: hashicorp/setup-terraform@v4
    - run: terraform init
    - run: terraform fmt -check
    - if: github.event_name == 'pull_request'
      run: terraform plan -no-color
    - if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: terraform apply -auto-approve -input=false
